@{

}
<h1> Jobs to do @Model.JobId</h1>
<div>
    <form asp-controller="Job" asp-action="PlanStitch" asp-route-date="@Model.date" >
        Delivery Date:
        <input type="date" name="date" value="@Model.date"/>
        <input type="submit"/> 
        <p>@Model.date</p>
    </form>
</div>
<div>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>JobNumber</th>
                <th>Quantity</th>
                <th>PaperInside</th>
                <th>PaperCover</th>
                <th>Machine</th>
                <th>Start this Job</th>
                <th>L-Stitch</th>
                <th>U-Stitch72</th>
                <th>U-Stitch48</th>
                <th>i-Sitch</th>
                <th>Out</th>
            </tr>
        </thead>
        <tbody>
            @{ foreach (var s in Model.jobLineList) {
                    <tr>
                        <td>@s.Job.JobNumber</td>
                        <td>@s.Job.Quantity</td>
                        <td>@s.Job.PaperBw</td>
                        <td>@s.Job.PaperCover</td>
                        <td>@s.MachineId</td>
                        <td>
                            <a asp-controller="Stitch" asp-action="StartStitch" class="btn btn-default"><span class="glyphicon glyphicon-record"></span></a>
                        </td>
                        <td>
                            <a asp-controller="Stitch" asp-action="PlanStitch" asp-route-id="@s.Id"  asp-route-date="@Model.date" class="btn btn-default"><span class="glyphicon glyphicon-record"></span></a>
                        </td>
                        <td>
                            <a asp-controller="Stitch" asp-action="PlanStitch" asp-route-id="@s.Id" asp-route-date="@Model.date" class="btn btn-default"><span class="glyphicon glyphicon-record"></span></a>
                        </td>
                        <td>
                            <a asp-controller="Stitch" asp-action="PlanStitch" asp-route-id="@s.Id"asp-route-date="@Model.date" class="btn btn-default"><span class="glyphicon glyphicon-record"></span></a>
                        </td>
                        <td>
                            <a asp-controller="Stitch" asp-action="PlanStitch" asp-route-id="@s.Id" asp-route-date="@Model.date" class="btn btn-default"><span class="glyphicon glyphicon-record"></span></a>
                        </td>
                        <td>
                            <a asp-controller="Stitch" asp-action="PlanStitch" asp-route-id="@s.Id" asp-route-date="@Model.date" class="btn btn-default"><span class="glyphicon glyphicon-record"></span></a>
                        </td>
                    </tr>
             }
            }
        </tbody>
    </table>
</div>

